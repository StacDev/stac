targets:
  $default:
    builders:
      stac_schema_builder|stac_schema_builder:
        enabled: true
      json_serializable:
        options:
          explicit_to_json: true
          field_rename: none
          include_if_null: true 